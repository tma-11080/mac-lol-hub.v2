getgenv().mac_config = {

    auraRange = 50,
    attackSpeed = 0.1,
    moveSpeed = 0.15,
    height = 5,

    playerESP = false,
    chestESP = false,
    followPlayer = false,
    followEnemy = false

}
